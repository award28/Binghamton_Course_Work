#ifndef LCS_HPP
#define LCS_HPP
#include <iostream>
#include <fstream>
#include <string>

class LenLCS {
    public:
        LenLCS() {}
        void execute(const std::string &x, const std::string &y, const std::string &outfile);
};
#endif
