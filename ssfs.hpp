#ifndef SSFS_HPP
#define SSFS_HPP
#include <iostream>

/*
class Clock {
    public:
        Clock(int arr[]);
        std::string execute(int cache);
    private:
        int accessed[numAccesses];
};
*/

#endif
